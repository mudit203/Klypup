ransform the flat table into a premium, airy, and organized data center.
Design Prompt:
"Refactor the 'Product Listing' dashboard to match a high-end, minimal SaaS aesthetic.
Layout & Structure: Implement a wide-margin layout using a soft grey background (#F9FAFB) with a faint 20px square grid overlay. The main content should be housed in a single, large 'Glassmorphic' container with a 16px border-radius, a 1px solid white border, and a soft ambient shadow.
Header & Filters: Replace the top bar with a clean horizontal header. The search bar should be a minimalist input field with an 'inner-glow' effect. Filter dropdowns should use a 'ghost' style (transparent background, thin border) and only show color on hover.
Table Typography & Rows: Use 'Inter' or 'SF Pro' font. Headers must be uppercase, 12px, tracking 0.05em, in a muted grey. Rows should have increased vertical padding (16px) with a subtle bottom-border divider.
Status Indicators: Refactor 'In-Stock' badges to be small, pills-shaped with low-opacity backgrounds (e.g., 10% green fill with 100% green text).
Color Scheme: Primary Black (#000000) for headers and 'Sign Out' buttons. Primary Blue (#007AFF) for interactive price links. Success Green (#10B981) for stock status."

Key Design Considerations to maintain "Production Standard":
Consistency: Ensure the "Sign In" button on the login page and the "Sign Out" or "Approval Queue" buttons on the dashboards use the exact same corner radius (8px or 12px) and font-weight.
Whitespace: Increase the "breathing room" between the data points. Professional dashboards feel premium because they aren't crowded.
Micro-interactions: Mention to your agent that all interactive elements (rows, buttons, chart points) should have a transition-all duration-200 ease-in-out effect.
The Grid: Notice in your 2nd image you have a light grid in the background. Keep this consistent across all pages to "lock" the design together visually